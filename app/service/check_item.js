'use strict';
const Service = require('egg').Service;

class CheckItemService extends Service {
  // async find() {
  //   return fake.list();
  // }

  // async createOne(data) {
  //   return fake.createOne(data);
  // }

  // async findOne({ id }) {
  //   return fake.findOneById(id);
  // }
}

module.exports = CheckItemService;
